# Dependency-scanner manifest.
#
# Runtime dependencies are intentionally empty in pyproject.toml. These entries
# mirror the optional provider packages and development tools declared there so
# Dependabot and other repository scanners have concrete Python packages to
# inspect.

anthropic>=0.69
openai>=1.0
anyio>=4.4.0
h11>=0.16.0
idna>=3.15
zipp>=3.19.1

build>=1.2.0
coverage[toml]>=7.6.0
hatchling>=1.25
mypy>=1.18.0
pytest>=8.4.0
ruff>=0.13.0
